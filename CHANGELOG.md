# 更新日志

## v1.2.0

新功能：

- 支持自定义发起重启和重启完成提示词。
- 增加重启耗时、前后内存状态、时间等提示词占位符。

问题修复：

- 重启完成通知改为在插件初始化后主动等待平台并重试发送，不再依赖一次性的 WebSocket 连接事件，修复平台启动时序竞争导致的偶发漏发。
- 通知发送成功后才清理重启缓存；超时失败时保留缓存，供下次启动继续尝试。

其他：

- 仓库维护者及安装链接更新为 `yun474/astrbot_plugin_restart`。

## v1.1.0

新功能：

- 在本地生成 JWT 用于仪表盘 API 的认证，而不是通过 HTTP 登录获取。

优化改进：

- 改进插件重载命令的匹配方式，支持对显示名称和内部名称的部分匹配。
