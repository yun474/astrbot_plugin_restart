# 更新日志

## 未发布

问题修复：

- 冷启动时在 AstrBot 核心加载完成后启动重启完成通知，避免插件、模型加载耗尽 120 秒重试窗口；插件热重载时仍可恢复待发通知。
- 通知任务只处理插件加载时已有的重启记录，避免误报当前进程中新发起的重启；重复生命周期事件不会重复创建任务，卸载时取消待发任务。
- 修复 QQ 官方机器人开启群独立会话后，原始群 ID 与隔离会话 ID 不一致导致的完成通知漏发；缺少发送上下文时保留缓存并记录错误。

## v1.2.1

问题修复：

- 保存并恢复 QQ 官方机器人适配器的会话场景与消息 ID，修复 AstrBot 重启后适配器内存缓存丢失，导致 QQ 群完成通知被静默跳过的问题。
- 兼容 QQ 官方机器人的普通 QQ 群和机器人私聊完成通知；QQ群需要开启机器人主动发言权限。

## v1.2.0

新功能：

- 支持自定义发起重启和重启完成提示词。
- 增加重启耗时、前后内存状态、时间等提示词占位符。

问题修复：

- 重启完成通知改为在插件初始化后主动等待平台并重试发送，不再依赖一次性的 WebSocket 连接事件，修复平台启动时序竞争导致的偶发漏发。
- 通知发送成功后才清理重启缓存；超时失败时保留缓存，供下次启动继续尝试。

其他：

- 仓库维护者及安装链接更新为 `yun474/astrbot_plugin_restart`。

## v1.1.0

新功能：

- 在本地生成 JWT 用于仪表盘 API 的认证，而不是通过 HTTP 登录获取。

优化改进：

- 改进插件重载命令的匹配方式，支持对显示名称和内部名称的部分匹配。
